# Optional — set to receive claim link via email on first run
ZERODB_EMAIL=

# Optional — skip auto-provisioning, use existing ZeroDB credentials
ZERODB_API_KEY=
ZERODB_PROJECT_ID=
ZERODB_BASE_URL=https://api.ainative.studio
