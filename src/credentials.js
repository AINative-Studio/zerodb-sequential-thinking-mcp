// Manages persistent ZeroDB credentials stored in the user's home directory.

export function readCredentials() {
  return null;
}

export function writeCredentials(data) {}

export function markClaimed() {}
